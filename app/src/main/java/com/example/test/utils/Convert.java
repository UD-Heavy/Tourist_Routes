package com.example.test.utils;

import android.media.Image;

/**
 *  Конвертирует строку в формате Base64 в объект изображения типа {@link Image}.
 *  Этот метод принимает строку Base64, декодирует её и возвращает
 *  объект {@link Image}, если декодирование успешно.
 *  @param 'base строка в формате Base64, представляющая изображение
 *  @return объект {@link Image}, созданный из строки Base64, или {@code null},
 *  если конвертация не удалась
 */
public class Convert {
    public static Image convertBase64ToImage(String base){

        return null;
    }
}